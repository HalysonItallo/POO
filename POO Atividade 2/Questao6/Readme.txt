a) O resultado dos dois "prints" é 90.0. Pois, quando ocorre a atribuição de c2 à c1 os dois objetos passam a apontar
para o mesmo endereço de memória, ou seja, as operações feitas em c1 implica no objeto c2.

b) Para objetos que não tem referência a Garbage Collection excluí.