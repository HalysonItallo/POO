Quando você tem que valor = valor + valor, o valor da esquerda
que é o que antes do "=" era sobrescrito pela soma dos valores.

Para concertar isso eu criei um atributo valor que guarda a soma
do valor com o próprio valor guardado no atributo.